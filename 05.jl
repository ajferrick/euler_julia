# Example
# We can determine this by hand: from a range, remove numbers with redundant
# division rules; e.g. 4 means that we can remove 2, and 8
println(reduce(*, [10, 9, 7, 4]))
println(reduce(*, [5, 9, 7, 11, 13, 16, 17, 19]))

